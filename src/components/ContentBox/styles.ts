import styled from "styled-components";

export const ContentBox = styled.textarea`
  background: var(--grey-100);
  border-radius: 15px;
  width: 100%;
  height: 120px;
  font-size: 13px;
  resize: none;
  padding: 20px;
`;
